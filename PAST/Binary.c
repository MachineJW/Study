#include<stdio.h>

void main(){
	printf("[컴퓨터란 무언인가?]\n");
	printf("누가 명령어를 읽는가?: CPU\n");
	printf("-CPU가 연산을 수행하기 위해서 저장하는 공간(레지스터)는 크기가 작다.\n");
	printf("-64비트 CPU의 경우 레지스터는 8바이트 짜리 총 16개.\n");
	printf("따라서 CPU가 연산을 하기위해서는 저장에 특화된 장치로 부터 명령어를 받는다.\n");
	printf("그 장치가 바로 RAM(메모리)\n");
	printf("메모리에 각방에는 고유의 주소값이 있다.(16진수)\n");
	printf("임의 접근 메모리\n");
	printf("RAM은 휘발성 메모리\n"); 
}
